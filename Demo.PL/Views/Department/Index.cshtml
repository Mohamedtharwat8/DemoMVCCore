@model IEnumerable<DepartmentViewModel>
@{
    ViewData["Title"] = "Index";
}

<h1>All Departments</h1>
<br />
<br />
@*
@if (@TempData["Message"] != null)
{
    <div class="my-4  alert alert-warning">
        @TempData["Message"]
    </div>
    
}
*@

<a asp-controller="Department" asp-action="Create" class="btn btn-primary">Create New Department</a>

<form method="post" asp-action="Index" class="mt-3">
    <div class="row justify-content-center align-items-center">
        <div class="col-8">
            <input type="text" placeholder="Search By Name .." class="form-control" name="searchValue" />
        </div>
        <div class="col-4">
            <button type="submit" class="btn btn-success">Search</button>
        </div>
    </div>
</form>

@if (Model.Count() > 0)
{
    <table class="table table-striped table-hover mt-3">
        <thead>
            <tr>
                <th>@Html.DisplayNameFor(d => d.Code)</th>
                <th>@Html.DisplayNameFor(d => d.Name)</th>
                <th>@Html.DisplayNameFor(d => d.DateOFCreation)</th>
                <th>Details</th>
                <th>Update</th>
                <th>Delete</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var department in Model)
            {
                <tr>
                    <td>@department.Code</td>
                    <td>@department.Name</td>
                    <td>@department.DateOFCreation</td>

                    <partial name="_ButtonsPartialView" model="@department.Id.ToString()"></partial>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <div class="mt-5 alert alert-warning">
        <h3>There is No Departments</h3>
    </div>
}






@*
<h1>Index</h1>
<h1>Department</h1>

<br />
<br />

<a asp-action="Create" asp-controller="Department" class="btn btn-primary">
    Create New Department</a>

<br />

@if (Model.Count() > 0 )
{
    <table class="table table-striped table-hover">
        <thead>
            <tr>

                <th>@Html.DisplayNameFor(d=>d.Code)</th>
                <th>@Html.DisplayNameFor(d=>d.Name)</th>
                <th>@Html.DisplayNameFor(d=>d.DateOFCreation)</th>
                <th>Details</th>
                <th>Update</th>
                <th>Delete</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var depart in Model)
            {
                <tr>
                    <td>@depart.Code</td>
                    <td>@depart.Name</td>
                    <td>@depart.DateOFCreation</td>
                    <td>
                        <a asp-action="Details" asp-controller="Department"
                        asp-route="depart.Id" class="btn btn-success">
                           <i class="fas fa-eye"></i> Details</a>
                    </td>
                    <td>
                        <a asp-action="Edit" asp-controller="Department"
                        asp-route="depart.Id" class="btn btn-warning">
                            <i class="fas fa-edit"></i> Update
                        </a>
                    </td>
                    <td>
                        <a asp-action="Delete" asp-controller="Department" 
                        asp-route="depart.Id" class="btn btn-danger">
                            <i class="fas fa-trash"></i> Delete
                        </a>
                    </td>
                </tr>
                
            }
        </tbody>
    </table>
}else{
    <div class="alert alert-warning"> 
        <p> There Is No Department</p>
    </div>
}*@

